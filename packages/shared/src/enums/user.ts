export enum UserEnum {
  ADMIN = 'admin',
  USER = 'user',
}

export enum UserStatusEnum {
  PENDING = 'pending',
  ACTIVE = 'active',
  BANNED = 'banned',
}

export enum UserRoleEnum {
  ADMIN = 'admin',
  USER = 'user',
}
